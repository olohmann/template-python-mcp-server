{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Lint (Ruff)",
            "type": "shell",
            "command": "ruff check --fix .",
            "problemMatcher": [
                "$ruff"
            ],
            "group": "build"
        },
        {
            "label": "Unit tests",
            "type": "shell",
            "command": "pytest -q",
            "group": "test"
        }
    ]
}
